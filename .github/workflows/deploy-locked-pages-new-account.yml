# DISABLED — NorCal locked space is Bryan + Grok only.
# Deploy from BelichickGillisMusk/norcal-LOCKED-2026-08-03, not this multi-agent repo.
# This workflow always fails so helpers cannot ship money-site Pages from here.

name: Deploy LOCKED Pages (new CF account)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Do not use — space is Bryan+Grok only on norcal-LOCKED repo'
        required: true
        default: ''

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Bryan + Grok only
        run: |
          echo "BLOCKED."
          echo "NorCal locked production is Bryan + Grok only."
          echo "Use repo BelichickGillisMusk/norcal-LOCKED-2026-08-03"
          echo "Claude / Cursor / helpers must not deploy from this repo."
          exit 1
