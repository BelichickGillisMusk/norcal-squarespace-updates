# Sync loop

Grok thinks / Cursor edits / GitHub stores / Cloudflare ships / Donger screen-proofs.

- Blog or snippet = files under `site/` only. PR. Preview. Merge.
- Never `wrangler deploy` from chat to norcalcarbmobile.com.
- Worker name stays `norcal-squarespace-updates-gillis`.
- `run_worker_first = true` is the outage mode. Path list only after schema/canary/redirects are in `site/`.
